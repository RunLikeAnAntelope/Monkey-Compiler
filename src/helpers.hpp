#include <string>
#include <vector>
namespace Helpers {
std::string combineVecStrWithDelim(std::vector<std::string> vecStr,
                                   const std::string &delimiter);
} // namespace Helpers
