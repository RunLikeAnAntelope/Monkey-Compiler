#include <string>
#include <vector>
namespace Helpers {
std::string combineVecStrWithDelim(std::vector<std::string> vecStr,
                                   std::string delimiter);
} // namespace Helpers
